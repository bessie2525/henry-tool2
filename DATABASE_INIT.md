# 数据库初始化指南

## 📋 步骤一：创建数据库集合

### 需要创建的集合列表

在云开发控制台中，手动创建以下集合（共16个）：

| 集合名称 | 说明 |
|---------|------|
| `users` | 用户表 |
| `students` | 学生档案 |
| `parents` | 家长档案 |
| `bindings` | 绑定关系表 |
| `pets` | 宠物表 |
| `tasks` | 任务表 |
| `task_submissions` | 任务提交表 |
| `shop_items` | 商店商品表 |
| `inventory` | 背包/库存表 |
| `coin_transactions` | 金币交易流水 |
| `achievements` | 成就配置表 |
| `achievement_records` | 成就记录表 |
| `chat_messages` | 宠物聊天记录 |
| `daily_themes` | 每日主题 |
| `daily_tasks` | 日常任务 |
| `stats_daily` | 每日统计表 |

---

### 创建集合的具体步骤

1. 打开微信开发者工具
2. 点击工具栏「云开发」按钮
3. 进入「数据库」标签页
4. 点击左侧「+」按钮添加集合
5. 输入集合名称（如 `users`）
6. 点击「确定」
7. 重复步骤4-6，创建所有16个集合

---

## 📋 步骤二：运行初始化云函数

我们已经有一个 `cloud/database/init.js` 云函数，它会自动初始化：
- 商店商品
- 成就配置

### 部署并运行初始化云函数

#### 1. 部署 database 云函数

在微信开发者工具中：
1. 找到 `cloud/database/` 文件夹
2. 右键点击该文件夹
3. 选择「上传并部署：云端安装依赖」
4. 等待部署完成

#### 2. 在云开发控制台测试云函数

1. 打开云开发控制台
2. 进入「云函数」标签页
3. 找到 `database` 云函数
4. 点击「测试」按钮
5. 在测试参数中输入：
   ```json
   {}
   ```
6. 点击「测试」按钮
7. 查看返回结果，确认初始化成功

---

## 📋 步骤三：配置数据库权限（可选但推荐）

虽然安全规则是可选的，但建议配置以提高安全性。

### 在云开发控制台配置权限

1. 打开云开发控制台
2. 进入「数据库」标签页
3. 选择一个集合（如 `users`）
4. 点击「权限设置」
5. 选择权限规则或自定义规则

### 推荐的权限配置

| 集合 | 权限设置 | 说明 |
|------|---------|------|
| `users` | 仅创建者可读写 | 用户只能读写自己的数据 |
| `students` | 仅创建者可读写 | 学生只能读写自己的档案 |
| `parents` | 仅创建者可读写 | 家长只能读写自己的档案 |
| `bindings` | 仅创建者可读写 | 家长只能读写自己的绑定关系 |
| `pets` | 仅创建者可读写 | 学生只能读写自己的宠物 |
| `shop_items` | 所有用户可读，仅管理员可写 | 商品公开可读 |
| `achievements` | 所有用户可读，仅管理员可写 | 成就配置公开可读 |
| 其他集合 | 仅创建者可读写 | 用户只能读写自己的数据 |

---

## 📋 步骤四：验证初始化结果

### 验证商店商品

1. 在云开发控制台进入「数据库」
2. 选择 `shop_items` 集合
3. 应该能看到 12 条商品数据

### 验证成就配置

1. 在云开发控制台进入「数据库」
2. 选择 `achievements` 集合
3. 应该能看到 6 条成就数据

---

## 🎉 初始化完成！

数据库初始化完成后，您就可以：
1. 在微信开发者工具中测试小程序
2. 注册学生/家长账号
3. 创建宠物
4. 测试各项功能

---

## 💡 常见问题

### Q: 必须手动创建所有集合吗？
A: 是的，微信云开发需要先创建集合才能写入数据。

### Q: 如果初始化云函数运行失败怎么办？
A: 检查：
1. 云函数是否成功部署
2. 集合是否都已创建
3. 云函数日志中的错误信息

### Q: 可以重新运行初始化吗？
A: 可以！初始化脚本会检查数据是否已存在，如果存在会跳过，不会重复创建。
